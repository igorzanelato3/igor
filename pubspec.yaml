name: service_app_go_fix
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  logger: ^2.5.0
  firebase_messaging: ^15.2.5
  firebase_core: ^3.13.0
  geolocator: ^14.0.0
  http: ^1.3.0
  cloud_firestore: ^5.6.6
  google_maps_flutter: ^2.12.1
  google_mobile_ads: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true